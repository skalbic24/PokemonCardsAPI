spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pokemon_cards_db
    username: pokemon_user
    password: pokemon_cards
    

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  sql:
   init:
    mode: never 
